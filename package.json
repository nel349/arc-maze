{
  "name": "arc-maze",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "description": "A maze on Arc that charges by the step, and pays out reputation.",
  "scripts": {
    "start": "bun run src/main.ts",
    "test": "bun test ./test",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.client.json --noEmit",
    "gate": "bun run typecheck && bun run test && bun run test:contracts",
    "test:contracts": "cd contracts && FOUNDRY_DISABLE_NIGHTLY_WARNING=1 forge test"
  },
  "dependencies": {
    "@circle-fin/x402-batching": "^3.4.0",
    "@x402/core": "^2.25.0",
    "@x402/evm": "^2.25.0",
    "viem": "^2.56.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^7.0.2"
  }
}
